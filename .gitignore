
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
# Solo use: auto-exported issues live in the Dolt DB (source of truth);
# JSONL is a viewer-only artifact (bv) and shouldn't be synced via git.
.beads/issues.jsonl

# macOS
.DS_Store

# Xcode (generated by xcodegen — regenerate with `xcodegen generate` in App/ and CLI/)
*.xcodeproj/
!**/*.xcworkspace/contents.xcworkspacedata
**/*.xcworkspace/xcuserdata/
**/*.xcworkspace/xcshareddata/swiftpm/
xcuserdata/
*.xcuserstate

# SwiftPM
.build/
.swiftpm/
Package.resolved

# Build artifacts
DerivedData/
build/
